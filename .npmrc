# Consistent dependency installation
fund=false
audit=false
save=false
legacy-peer-deps=false

# Prefer workspaces for monorepo
workspaces=false

# Use the system Node version (as specified in .nvmrc)
