/**
 * Toast notification system.
 *
 * {@link ToastProvider} — Context provider that renders a toast overlay and
 * manages toast lifecycle (add, dismiss, auto-dismiss).
 *
 * {@link useToast} — Hook for showing toasts from any child component.
 */
export { ToastProvider, useToast } from './toast';
